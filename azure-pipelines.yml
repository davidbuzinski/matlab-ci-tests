# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo "##[warning]This version of setup-matlab is deprecated. Please upgrade!" 
  displayName: 'Setup MATLAB'

- script: echo "##vso[task.logissue type=warning]This version of setup-matlab is deprecated. Please upgrade!"
  displayName: 'Setup MATLAB'
